import { HugeiconsIcon } from "@hugeicons/react";
import { ArrowRight01Icon, InformationCircleIcon } from "@hugeicons/core-free-icons";
import { Button } from "../components/ui/Ui";

type ErrorStatePageProps = {type: "not-found" | "technical";onBack: () => void;onSupport: () => void;};
export function ErrorStatePage({ type, onBack, onSupport }: ErrorStatePageProps) {
  const missing = type === "not-found";
  return <main className="grid min-h-screen place-items-center bg-sand p-4"><section className="w-full max-w-md rounded-[26px] border border-line bg-white p-8 text-center shadow-[0_16px_60px_rgba(23,32,51,.07)]"><span className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-pine-50 text-pine-700"><HugeiconsIcon icon={InformationCircleIcon} size={26} /></span><p className="mt-6 text-xs font-bold uppercase tracking-[.14em] text-pine-700">{missing ? "Erreur 404" : "Service temporairement indisponible"}</p><h1 className="mt-3 text-3xl font-bold tracking-[-.05em]">{missing ? "Cette page est introuvable." : "Un incident technique est survenu."}</h1><p className="mt-3 text-sm leading-6 text-ink-soft">{missing ? "Le lien peut être expiré ou l’adresse incomplète. Vérifiez le lien puis réessayez." : "Notre équipe a été prévenue. Vos données et votre file ne sont pas affectées."}</p><div className="mt-7 flex flex-col gap-3 sm:flex-row"><Button onClick={onBack} className="flex-1" icon={<HugeiconsIcon icon={ArrowRight01Icon} size={17} />}>Retourner à l’accueil</Button><Button onClick={onSupport} variant="secondary" className="flex-1">Contacter le support</Button></div></section></main>;
}