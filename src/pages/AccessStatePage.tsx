import { HugeiconsIcon } from "@hugeicons/react";
import { ArrowRight01Icon, InformationCircleIcon, LockPasswordIcon } from "@hugeicons/core-free-icons";
import { Button } from "../components/ui/Ui";

export function AccessStatePage({ onSupport, onBack }: {onSupport: () => void;onBack: () => void;}) {
  return <main className="grid min-h-screen place-items-center bg-[#f7f7f5] p-4"><section className="w-full max-w-lg rounded-[26px] border border-[#e4e4de] bg-white p-7 text-center shadow-[0_16px_60px_rgba(23,32,51,.07)] sm:p-9"><span className="mx-auto grid h-12 w-12 place-items-center rounded-full bg-[#fff1e5] text-[#c45b1a]"><HugeiconsIcon icon={LockPasswordIcon} size={25} /></span><p className="mt-6 text-xs font-bold uppercase tracking-[.14em] text-[#c45b1a]">Accès temporairement limité</p><h1 className="mt-3 text-3xl font-bold tracking-[-.055em] text-[#172033]">Votre abonnement nécessite une action.</h1><p className="mx-auto mt-4 max-w-md text-sm leading-6 text-[#687385]">Les données de Centre Serein sont conservées. Réactivez votre abonnement pour reprendre les inscriptions par QR code.</p><div className="mt-7 rounded-xl border border-[#f0d8bd] bg-[#fffbf6] p-4 text-left"><div className="flex gap-3"><HugeiconsIcon icon={InformationCircleIcon} size={18} className="shrink-0 text-[#c45b1a]" /><p className="text-xs leading-5 text-[#86572f]">Le paiement du 14 juillet n’a pas été confirmé. Mettez à jour votre moyen de paiement ou contactez le support.</p></div></div><div className="mt-7 flex flex-col gap-3 sm:flex-row"><Button onClick={onBack} className="flex-1" icon={<HugeiconsIcon icon={ArrowRight01Icon} size={17} />}>Mettre à jour le paiement</Button><Button onClick={onSupport} variant="secondary" className="flex-1">Contacter le support</Button></div></section></main>;
}